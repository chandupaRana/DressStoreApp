const express = require('express');
const router = express.Router();
const Category = require('../models/category');

// Define your routes for categories (GET, POST, PUT, DELETE) here.

module.exports = router;
